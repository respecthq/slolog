# 機種画像の入れ方

許諾を得た筐体画像を `public/images/machines/` に保存し、対象の `src/content/machines/機種名.md` の冒頭へ次の項目を追加します。

```yaml
image:
  src: images/machines/機種名.webp
  alt: 機種名の筐体
  placement: above
  credit: メーカー指定のクレジット表記
```

- `placement: above`：タイトルの上に表示。筐体全体が見えるように配置します。
- `placement: background`：タイトル背景の右側に表示。文字の読みやすさを保つため暗く重ねます。
- 画像を登録すると、機種一覧とLPの機種紹介にも表示されます。
- `credit` は任意です。メーカーの指定があれば記入してください。
- 画像がない機種は `image` 項目を省略します。空の画像枠は表示されません。
- PNG / JPG / WebP / AVIF 対応。透過PNGまたはWebPが筐体には適しています。
- 画像をアプリ用JSONには含めません（現在のアプリの読込形式を維持）。
- 存在しない画像パスと空の代替テキストはビルド時に検出します。

許諾管理資料はサイトの公開フォルダに置かず、必要な表示クレジットだけを記入してください。
