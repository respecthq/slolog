# 機種情報パイプライン

新台が「検定通過」から「LPに完全な機種ページができる」までの流れと、どの項目をどこから取るかの規約。

> **2026-09-23 決定：天井・天井恩恵・ゾーン・設定変更時・天井なしは LP で扱わない。** メーカー非公表の解析値で、条件の注釈が漏れるとクレームにつながるため。機種ページに欄を出さず、machines.json でも空で送る（アプリの天井・ゾーン欄はユーザーが入力する）。実機に値を入れるとビルドが落ちる。以下の手順のうち天井に関する部分は無効。

## 3段階

機種の情報は一度に揃わない。揃った順に段階を上げる。**ページは段階1で1回作り、段階2で追記するだけ。URL（スラッグ）は変えない。**

| 段階 | トリガー | LPでの扱い | 埋まる項目 |
|---|---|---|---|
| **0. 検定通過** | 業界ニュースで判明 | **機種ページは作らない。** 週次レポートの「検定通過」欄に載せるだけ | 機種名・メーカー（導入日は未定） |
| **1. メーカー公開** | メーカーが機種サイトを公開 | **機種ページを作る**（`draft: false`） | 基本情報＋基本スペック |
| **2. 解析確定** | 導入後、法人2媒体に天井が出そろう | 同じページに**天井ブロックを追記** | 天井・恩恵・設定変更 |

### 段階0でページを作らない理由

LPは「アプリに取り込む機種データ」であって、機種の総合情報サイトではない。検定通過時点では機種名とメーカーしか無く、取り込んでも意味がない。加えて、導入中止・名称変更・導入日変更が普通に起きる。
**スラッグは永久IDなので、名称が固まる前にページを作るのが一番まずい。**

### 段階1のトリガーが「メーカー公開」である理由

`content.config.ts` の refine が「`draft: false` には `source`（メーカー公式URL）が必須」を強制している。
メーカーが機種サイトを出すまで、そもそも配信できない。この制約とパイプラインが一致している。

## どこから取るか

線引きは **「メーカーが公表しているか」の一点**。公表しているものは必ずメーカーから取る（媒体からは取らない）。

| 項目 | 取得元 | 備考 |
|---|---|---|
| 機種名・メーカー・導入日 | **メーカー公式** | `source` に入れる一次情報 |
| タイプ・分類（号機）・筐体 | **メーカー公式** | 機種サイトに記載あり |
| 設定別スペック（BB/RB/合成/出玉率） | **メーカー公式** | 北電子＝HTML（`fetch-kitadenshi.mjs`）／大都・ユニバ＝画像（手入力） |
| 純増・ボーナスのゲーム数 | **メーカー公式** | 大都は `info1_gameflow.jpg` に記載。載っていない機種もある（秘宝伝は獲得枚数のみ） |
| 配当表・リール配列 | **メーカー公式** | 画像。参考情報 |
| **天井・天井恩恵** | **パチガブ**（必勝本でクロス確認） | どのメーカーも非公表 |
| **ゾーン・モード別G数** | **パチガブ**（同上） | 同上 |
| **設定変更時の挙動** | **パチガブ**（同上） | 同上 |
| 狙い目・ヤメ時・期待値 | **扱わない** | 利用規約と product 方針 |

**同じ数値でもメーカーが出しているならメーカーから取る。** そうすれば媒体に依拠する項目が最小化され、`verified` を付ける対象が天井まわりだけに絞られる。

## 機種ページのテンプレ

**出所ごとにブロックを分ける。** 読者が「どれがメーカー公表で、どれが媒体確認か」を一目で判別できることが、この構成の目的。

```
── 基本情報 ──────────────  出所：メーカー公式
   メーカー / 導入日 / タイプ / 分類 / 筐体
   出典：メーカー・公式情報を開く（取得日）

── 基本スペック ──────────  出所：メーカー公式
   純増 / ボーナス合成確率 / 出玉率
   （空なら「メーカーが公表していないため掲載していません」）

── 天井・ゾーン ──────────  出所：メーカー非公表・媒体で確認
   天井 / 天井恩恵 / 設定変更時
   確認：777パチガブ（YYYY-MM-DD時点）
   ※ 各媒体との提携関係はありません
   （空なら「解析待ち」）

── 設定別スペック ────────  出所：メーカー公式
   設定1〜6の表

── 本文 ────────────────
   ゲーム性の特徴など（自分の言葉）

── この機種をスロログに追加 ──
   機種データをコピー
```

### 書き方の規約

数値・単位・状態名はそのまま書いてよい。説明文は書き直す。**表の見出しと構造は媒体を真似ない。**
詳細は `SOURCES.md`「何をそのまま書いてよく、何を書き直すか」。

## 週次ルーティンとの分担

クラウドの週次ルーティン（毎週月曜7時JST）は **WebFetch が403で使えない**ので、数値の取得はしない。

| | クラウド（週次） | ローカル（手動） |
|---|---|---|
| 検定通過の検知 | ○ | — |
| 新台・導入日の検知 | ○ | — |
| メーカー機種サイトのURL収集 | ○（`maker_page_url`） | — |
| 設定別スペックの取得 | × | ○ `node scripts/fetch-kitadenshi.mjs` ／ 画像から手入力 |
| 天井の2媒体クロス確認 | ×（URLを出すだけ） | ○ curl で両媒体を取得して照合 |
| LPへの反映・コミット | × | ○ |

## 並び順と「最近の更新」

**機種一覧の並びは導入日（`released`）の降順。** ページを作った順ではない。
ページを先に作っても導入日が古ければ下に沈むし、あとで作っても導入日が新しければ上に来る。
一覧は「機種のカタログ」であって「サイトの更新履歴」ではないので、これが正しい。

**「うちが新しく追加/更新したもの」はホームの「最近の更新」が受け持つ。** 役割を分けてある。

| フィールド | 意味 | 誰が書くか |
|---|---|---|
| `released` | 機種の導入日。一覧の並び順に使う | メーカー公式 |
| `added` | LPにページを作った日 | ページ作成時に手で書く |
| `updated` | 最後に内容を更新した日（任意） | 内容を足したとき |
| `updateNote` | 新着に出す一言（例 `天井・ゾーンを追加`） | 同上 |

「最近の更新」は `updated || added` の降順で5件。`updated` があれば `updateNote`、無ければ「新規追加」と出る。
**新しい機種ページを作ったら `added` を必ず書く。** 書かないと新着に出ない。

## 転記ミスの検出（crosscheck.mjs）

`node scripts/crosscheck.mjs [slug ...]` で、うちのデータを必勝本の該当ページ群と突き合わせる。
**相手の数値を借りるためではなく、こちらの誤りを見つけるためのQAツール。**

照合するのは メーカー名 / 導入日 / 出玉率 / 純増 / 天井 の5項目。
機種トップに加えて「基本スペック」「天井&設定変更」「通常時解説」「AT解説」など最大9ページを取得して
ひとつの干し草の山にしてから探す（数値がどのページにあるかは機種ごとに違うため）。

**機種を追加・更新したら必ず回す。** これまでに実際の誤りを9件見つけている
（メーカー名4件・導入日3件・天井1件・純増2件）。

## 公式の情報が増えたことに気づく（watch-sources.mjs）

導入前の機種は、公式に「発売・製造元・導入日」しか出ていないことが多い。あとから純増・確率表・天井などが公表されたことに気づくための見張り。

```
node scripts/watch-sources.mjs         # 前回と比べて、変化のあった機種だけ出す
node scripts/watch-sources.mjs --all   # 全機種の現在の状態
```

- 見るページは各機種の `source` と、frontmatter の `watch:`（製品ページなど。リリースと製品ページが別のメーカー向け）
- **★ が付いたら公式ページを読みに行く**：これまで無かった語（純増・天井・出玉率・設定・確率…）が出た／表が増えた／スペックらしい名前の画像が増えた
- △ は本文の長さが変わっただけ（お知らせ欄の入れ替えでも起きる）。参考程度
- 数値は自動で取り込まない。載せるかどうかは出典を読んで決める（北電子の確率表だけは `fetch-kitadenshi.mjs` が取り込む）
- 前回の状態は `notes/source-snapshots.json`（非公開）。**週次の検証（shindai-verify）のたびに回す**

### 天井の見張り（同じスクリプトの後半）

天井はメーカーが公表しない。載せられるのは 777パチガブ と 必勝本 の 2 媒体で一致したときだけなので、この 2 サイトも見張る。対象は「天井が空で、`noCeiling` でもない」機種。

- frontmatter の `watch:` に 2 媒体の機種ページ URL を書いておくと、**そのページに天井の記載が出たとき**に ★ で知らせる。2 媒体そろうと ★★（照合して載せられる）
- URL がまだ無い機種は、機種名で 2 媒体を検索して**ページができたとき**に候補を出す。**候補は人が確かめてから `watch:` に足す**（同名の旧機種・パチンコ版が混ざる。クラウドの下書きが獣王・ツインエンジェル2で旧機種の URL を出した例あり）。検索語を変えたいときは `watchKeyword:`
- 候補は番号が `minId` 以上のページだけ（旧機種よけ）。年に一度、スクリプト内の `minId` をその年の新台の番号に合わせる
- ★★ が出たら：2 媒体の数値が一致するか読む → `ceiling` / `ceilingBonus` と `verified`（by・url・date・crosscheck）を入れる → `node scripts/crosscheck.mjs <slug>`


## 進行表（notes/LP_BOARD.md）と OK の流れ

ユーザーが毎週ざっと確認するための一覧。`node scripts/lp-board.mjs` で作り直す（週次の検証の最後に必ず回す）。

| 段 | 中身 | 元データ |
|---|---|---|
| 🟠 OK 待ち | 出典を見て OK を出せば、すぐ反映できるもの（NEW＝新しいページ／追加情報＝前からある機種の更新） | `notes/lp-proposals.json` |
| ✨ この 7 日の動き | 直近 7 日の NEW と追加情報の一覧。各表の機種名にも同じ印が付く | 下の「NEW と追加情報」 |
| 🔵 候補 | まだページがない機種（検定通過／メーカー公開／導入済み・未掲載）と、次にやること | `notes/lp-queue.json` |
| 🟡 更新待ち | ページはあるが、情報がまだ増える（導入前／公表値がまだ／天井がまだ／下書き） | 機種 md から自動判定 |
| 🟢 完了 | 情報が出そろった。以後は見張りだけ | 同上 |

- **完了の条件**：型式名あり＋天井が決着（あり／`noCeiling`）＋公表値が決着（掲載済み／`specNone: true`／導入から 60 日たっても未公表）＋導入済み
- **提案の作り方**（Claude）：新規は機種 md を `draft: true` で作り、`lp-proposals.json` に `{id:"A1", kind:"new", slug, name, summary, source}` を足す。更新は `{id, kind:"update", slug, name, summary, changes:{ceiling:…, verified:{…}}, source, crosscheck, updateNote}`。**必ず出典 URL を付ける**
- **OK が出たら**：`node scripts/lp-apply.mjs A1 A3`（全部なら `all`）→ `npm run build` → `node scripts/crosscheck.mjs <slug>` → `node scripts/verify-editorial.mjs dist` → push → `node scripts/lp-board.mjs`
- 候補がページになったら `lp-queue.json` から消す。`notes/` は非公開
- **区分の正は週次レポート**（2026-09-23〜）：クラウドの週次ルーティンが LP のリポジトリを読み、機種ごとに 🆕 NEW／➕ 追加情報／＝ 既報 を付けてくる。受け取ったら `node scripts/weekly-import.mjs <レポート.md> --session <URL>` で取り込む（notes/weekly/<日付>_draft.md に保存し、index.json の items に区分を入れる。候補にある機種の NEW は「候補の続報」に補正）。進行表はいちばん新しいレポートの区分を印にし、レポートに載った機種は既報も含めてレポートの判定だけを使う。レポートに無い LP 側の更新だけ、下の記録で補う
- **NEW と追加情報**（2026-09-23〜）：ユーザーが「完全に新しい情報か、前からある機種の続報か」を見分けるための印。記録を付け忘れると印が出ないので必ず書く
  - 機種ページを新しく作ったら frontmatter の `added: YYYY-MM-DD`（LP に載せた日）→ NEW
  - 前からある機種に情報を足したら `updated: YYYY-MM-DD` と `updateNote`（例「天井・ゾーンを追加」）→ 追加情報。`lp-apply.mjs` の update は自動で付く。手で直したときも付ける
  - 候補（`lp-queue.json`）は新しく積んだら `foundAt`、あとで情報が増えたら `updatedAt` と `updateNote`
  - 保険：`updated` を付け忘れても、7 日前の版（git）と中身の項目（天井・導入日・型式名・公表値・出典・公開など）を比べて変わっていれば「追加情報（更新メモなし）」と出る。見張り先 `watch` などの裏方の項目は数えない

## 型式名（modelName）の取り方

- 型式名はメーカーの製品ページには載っていない。公安委員会の検定公示（型式試験の適合・検定通過の公示）に載る正式名で、
  P-WORLD の機種DB（`https://www.p-world.co.jp/machine/database/<id>`）が「型式名」「検定番号」として転記している。
- 新しい機種を `src/content/machines/` に足したら、同じ週のルーティンで P-WORLD 機種DBを引き、
  `modelName`（表記はDBのまま。「L／真打吉宗／A1」の斜線も残す）、`modelNameSource`（DBのURL）、`kenteiNo`（検定番号）を frontmatter に入れる。
- 型式名の末尾の記号はメーカー記号（KK/KT/KA/KD/ZT/ZN/TN/TT＝北電子、A1〜A3＝大都技研、PA1〜PA7＝パオン・ディーピー、SB/SC＝サボハニ、BD＝ユニバーサルブロス）。
  **maker と食い違ったら maker を疑う**（例：スマスロ秘宝伝は PA7＝パオン・ディーピー製、大都技研は販売）。
- 表示は機種ページの基本情報「型式名」の1行だけ。検定番号と確認元URLはページに出さない（照合用）。

## LPのアプリ画面を撮り直したとき

スロログ側で `flutter drive --driver=test_driver/integration_test.dart --target=integration_test/shots_test.dart -d <simulator>` を回して
`~/slolog/screenshots/raw/` が更新されたら、LP側でこれを実行する。

```
node scripts/refresh-screens.mjs
```

`public/editorial/assets/` の home / ev / graph / record / calendar / review / review-detail を作り直す。
サンプルの筐体画像を差し替えたときも、これを回さないとLPだけ古い絵のまま残る。
